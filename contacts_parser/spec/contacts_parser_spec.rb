RSpec.describe ContactsParser do
  it "has a version number" do
    expect(ContactsParser::VERSION).not_to be nil
  end

  # it "displays usage info" do
  #   usage = ContactsParser::usage()
  # 
  #   expect(usage.index("Usage")).not_to be nil
  # end
end
